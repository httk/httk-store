[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "httk-store"
version = "2.1.0"
description = "httk₂ module for data management: entry providers, property-definition validation, and database storage."
readme = "README.md"
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [{ name = "Rickard Armiento", email = "gitcommits@armiento.net" }]
dependencies = [
  "httk-core>=2.1.0,<3",
  "jsonschema>=4",
]
keywords = [
  "Materials Discovery",
  "Materials Informatics",
  "Materials Science",
  "Machine Learning",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Chemistry",
  "Topic :: Scientific/Engineering :: Physics",
]

[project.urls]
Homepage = "https://httk.org"
Documentation = "https://docs.httk.org/httk-store/"
Repository = "https://github.com/httk/httk-store"
Issues = "https://github.com/httk/httk-store/issues"

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-xdist>=3.6",
  "ruff>=0.16,<0.17",
  "pyright",
  "mypy",
  "pydoclint>=0.9,<1",
  "httk-store[clickhouse,duckdb,mongodb,parallel]"
]
docs = [
  "sphinx",
  "furo",
  "myst-parser",
  "myst-nb",
  "sphinx-autodoc-typehints",
  "sphinx-copybutton",
  "sphinx-autoapi"
]
release = [
  "build>=1.2",
  "twine>=6.0",
]
default = ["httk-store[db]"]
db = ["sqlalchemy>=2.0,<3"]
# Pandas avoids repeated failed import probes during DuckDB scalar binding.
duckdb = ["httk-store[db]", "duckdb_engine>=0.13", "duckdb>=1.0", "numpy", "pandas"]
# clickhouse-connect 1.6.0 was probed for G0/P5; keep the compatible major range.
clickhouse = ["httk-store[db]", "clickhouse-connect>=1.6,<2"]
postgresql = ["httk-store[db]", "psycopg[binary]>=3.1"]
parallel = ["pyarrow>=15"]
mongodb = ["pymongo>=4.6"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.setuptools.package-data]
"httk.store" = ["py.typed"]
"httk.registry.entries.store" = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-n 4 --dist loadgroup -m 'not extended'"
markers = [
  "extended: full-depth test case; selected only by the extended test profile",
]

[tool.ruff]
target-version = "py312"
src = ["src", "examples"]
line-length = 120

[tool.ruff.lint]
# TRY002: raising plain Exception is accepted httk style.
# TRY004: the ValueError/TypeError choice is public API; do not churn it.
# BLE001: broad excepts deliberately isolate plugin/loader/handler failures.
ignore = ["TRY002", "TRY004", "BLE001"]

[tool.ruff.format]
# preserve replaces black's skip-string-normalization: existing quote styles are kept.
quote-style = "preserve"

[tool.pydoclint]
style = "sphinx"
arg-type-hints-in-docstring = false
check-return-types = false
check-yield-types = false
check-class-attributes = false
skip-checking-raises = true

[tool.pyright]
pythonVersion = "3.12"
include = ["src", "examples"]
# The examples import httk as an installed package would, so point pyright at
# the in-tree sources; ../httk-serve/src resolves the cross-repo serving
# example (httk-serve is a consumer of this package, never a dependency of
# it) against its workspace checkout, and is harmless where that path does not
# exist. Listing extraPaths explicitly disables pyright's implicit use of ./src,
# so "src" is listed too.
extraPaths = ["src", "../httk-serve/src"]
# Resolve the httk-core runtime dependency (and jsonschema) against the shared
# workspace venv, where httk-core is installed editable; pyright falls back to
# default resolution where that venv does not exist (e.g. CI, where the deps are
# installed into the job env).
venvPath = ".."
venv = ".venv"
reportMissingImports = true

[tool.mypy]
python_version = "3.12"
files = ["src", "examples"]
mypy_path = ["src"]
namespace_packages = true
explicit_package_bases = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
# jsonschema (a hard runtime dependency) ships no inline type information; we do
# not vendor its optional third-party stubs, so treat its imports as untyped.
module = ["jsonschema", "jsonschema.*"]
ignore_missing_imports = true
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
# httk-serve is a *consumer* of this package, not a dependency of it: the two
# meet only at the httk.core.EntryProvider contract. The cross-repo example
# (examples/serve_as_optimade.py) imports it to show the serving end of that
# contract, so its import must typecheck whether httk-serve is absent
# entirely or present without a py.typed marker.
module = ["httk.serve.optimade", "httk.serve.optimade.*"]
ignore_missing_imports = true
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
# pyarrow (an optional ClickHouse bulk-ingest dependency) ships no py.typed
# marker and we do not vendor its stubs, so treat its imports as untyped.
module = ["pyarrow", "pyarrow.*"]
ignore_missing_imports = true
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
# clickhouse-connect's SQLAlchemy type module re-exports its type vocabulary
# through `from ... import *` with a dynamically computed __all__, so the type
# names (Bool, Int64, Nullable, ...) are invisible to static import following.
# Skip following it and treat the module as untyped rather than annotate every
# access site; pyright resolves the star-import independently and is unaffected.
module = ["clickhouse_connect.cc_sqlalchemy.types"]
follow_imports = "skip"
